get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

find_dependency(Threads REQUIRED)

if(NOT TARGET @BEHAVIOR_TREE_LIBRARY@)
    include("${SELF_DIR}/@BEHAVIOR_TREE_LIBRARY@-targets.cmake")
endif()

set(@BEHAVIOR_TREE_LIBRARY@_INCLUDE_DIRS @CMAKE_INSTALL_PREFIX@/@INCLUDE_INSTALL_PATH@)
set(@BEHAVIOR_TREE_LIBRARY@_LIBRARIES @BEHAVIOR_TREE_LIBRARY@)
